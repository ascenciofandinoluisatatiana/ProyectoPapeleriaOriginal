services:
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    networks:
      - red_apps

  django-app:
    depends_on:
      redis:
        condition: service_started

  django-app-copy:
    depends_on:
      redis:
        condition: service_started